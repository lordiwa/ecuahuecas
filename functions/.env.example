# Copy to functions/.env (gitignored) and set the first admin's email.
# bootstrapAdmin grants { admin: true } ONLY to a signed-in user whose verified
# email matches this value. This is server-side config, never shipped to browser.
ADMIN_EMAIL=you@example.com
